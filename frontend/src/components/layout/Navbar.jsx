import ThemeToggle from "../common/ThemeToggle";

const Navbar = () => {
  return (
    <div className="navbar">
      <h2>HandMotion Play</h2>
      <ThemeToggle />
    </div>
  );
};

export default Navbar;